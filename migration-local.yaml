name: myapp-local-refresh
description: "Local docker-compose refresh"

source:
  strategy: docker_full
  host: local
  app: myapp

target:
  strategy: docker_full
  host: local
  app: myapp
  remote_dir: .
  verify_url: http://localhost:8000/health
  env_file: .env
